id: https://example.org/dedupe/commands/create_image_priority_manifest
name: dedupe-create_image_priority_manifest-command
title: create_image_priority_manifest Command
description: Creates prioritized manifest for upload
prefixes:
  linkml: https://w3id.org/linkml/
  dedupe: https://example.org/dedupe/
default_prefix: dedupe
default_range: string
imports:
- linkml:types
- ../../../../../dizzy/src/dizzy/def/commands
classes:
  create_image_priority_manifest:
    description: Creates prioritized manifest for upload
    is_a: Command
    attributes:
      command_id:
        description: Unique identifier for this create_image_priority_manifest command
        required: true
        range: string
