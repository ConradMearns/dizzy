id: https://example.org/dedupe/events/scan_item_found
name: dedupe-scan_item_found-event
title: scan_item_found Event
description: Found a file (not folder) while scanning
prefixes:
  linkml: https://w3id.org/linkml/
  dedupe: https://example.org/dedupe/
default_prefix: dedupe
default_range: string
imports:
- linkml:types
- ../../../../../dizzy/src/dizzy/def/events
classes:
  scan_item_found:
    description: Found a file (not folder) while scanning
    is_a: Event
    attributes:
      event_id:
        description: Unique identifier for this scan_item_found event
        required: true
        range: string
