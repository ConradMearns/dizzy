id: https://example.org/dedupe/events/manifest_upload_started
name: dedupe-manifest_upload_started-event
title: manifest_upload_started Event
description: Upload has started for a manifest item
prefixes:
  linkml: https://w3id.org/linkml/
  dedupe: https://example.org/dedupe/
default_prefix: dedupe
default_range: string
imports:
- linkml:types
- ../../../../../dizzy/src/dizzy/def/events
classes:
  manifest_upload_started:
    description: Upload has started for a manifest item
    is_a: DomainEvent
    attributes:
      event_id:
        description: Unique identifier for this manifest_upload_started event
        required: true
        range: string
      occurred_at:
        description: Timestamp when this event occurred
        required: true
        range: datetime
