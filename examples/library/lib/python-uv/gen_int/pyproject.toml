[project]
name = "gen_int"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gen_def",
]

[tool.uv.sources]
gen_def = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
