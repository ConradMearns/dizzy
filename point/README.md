Based on `sim/PLAYBOOK.md` and the scenarios in the `sim/` folder.
We are going to start hand programming the simulator system!

Claude might take over.

The goal is to ensure we have a simulator that can operate over executable and simulated features.

