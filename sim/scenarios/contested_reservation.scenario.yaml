description: >
  The interesting path — one copy, two members, a reservation, and a return.
  Exercises the full c→d→e→y→c loop: the return should trigger
  fulfill_reservation_on_return, which dispatches borrow_book for the head of
  the queue. This scenario is expected to surface findings: the policy needs
  reservation data it cannot query, and lend_book must arbitrate between a
  walk-in and the queue head.

context:
  - >
    Minting Server codes are assigned: Online = 01, Front Desk = 02.
  - >
    A pre-existing Test User exists with member id 0042-00-00000000,
    set up by a technician before the story begins.

commands:
  - register_member: Ada joins the library at the Front Desk
  - register_member: Grace joins the library Online
  - add_book: One copy of "A Discipline of Programming" arrives
  - borrow_book: Ada presents her member id and borrows it (shelf now empty)
  - reserve_book: Grace presents her member id and reserves it
  - return_book: Ada returns it — the policy should now act for Grace
